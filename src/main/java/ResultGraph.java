import java.util.List;

class ResultGraph {
    List<Node> nodes;
    List<Link> links;

    ResultGraph(List<Node> nodes, List<Link> links) {
        this.nodes = nodes;
        this.links = links;
    }
}
